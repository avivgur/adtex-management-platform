# Demo mode is safe and requires no external credentials.
ADTEX_DATA_MODE=synthetic

# Production adapters are intentionally not implemented in this portfolio edition.
# Add provider credentials only in a private deployment and never commit them.
MONDAY_API_TOKEN=
GOOGLE_SERVICE_ACCOUNT_JSON=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PERFORMANCE_API_BASE_URL=
PERFORMANCE_API_TOKEN=
